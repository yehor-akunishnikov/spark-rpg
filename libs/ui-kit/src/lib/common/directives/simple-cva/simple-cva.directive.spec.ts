import { SimpleCvaDirective } from './simple-cva.directive';

describe('SimpleCvaDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleCvaDirective();
    expect(directive).toBeTruthy();
  });
});
