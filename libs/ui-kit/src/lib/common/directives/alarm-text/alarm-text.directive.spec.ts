import { AlarmTextDirective } from './alarm-text.directive';

describe('AlarmTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AlarmTextDirective();
    expect(directive).toBeTruthy();
  });
});
